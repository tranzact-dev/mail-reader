# メール読み上げアプリ インストール手順

## 前提条件

- Windows 10 または Windows 11
- インターネット接続

## インストール（自動）

### 事前準備（開発者側）

ヘルパーさんにメールで以下の2ファイルを送る:

1. **install.bat.txt** — install.bat の拡張子を `.txt` に変更して添付（Gmailが `.bat` をブロックするため）
2. **.env** — 設定済みのものを用意

`.env` の内容:
```
ANTHROPIC_API_KEY=（Claude APIキー）
BIGLOBE_EMAIL=（BIGLOBEメールアドレス）
BIGLOBE_PASSWORD=（BIGLOBEパスワード）
CASUAL_CONTACTS=
```

### 実行（ヘルパーさん）

1. 父のアドレスにメールでインストール資材を送付済です。メールの添付ファイル（install.bat.txt と .env）を同じフォルダ（デスクトップでOK）に保存してください。
2. .envファイルをメモ帳で開き、APIキーの一部を修正します。末尾部分の`...pPcwQ-5wzKVg`の後に`AA`を追加し、`pPcwQ-5wzKVgAA`としてください。
3. `install.bat.txt` の拡張子 `.txt` を削除して `install.bat` にリネームしてください。
4. `install.bat` を右クリック →「管理者として実行」で実行してください
5. 完了を待つ（5〜10分）

install.bat が自動で以下を実行します:
1. Python のインストール状況を確認 → なければ自動でダウンロード＆サイレントインストール
2. Git のインストールを確認 → なければ自動でダウンロード＆サイレントインストール
3. GitHubからアプリを `C:\mail-reader` に取得
4. 依存パッケージをインストール（pip install）
5. `.env` を配置
6. デスクトップに「メール読み上げ」ショートカットを作成


こちらの環境では、Step1.のPythonインストール直後にエラー終了することがありました。その場合はもう一度batファイルを実行すると通ると思います。

通らなかったら連絡ください。


### アプリの起動

デスクトップの「メール読み上げ」をダブルクリック。
デスクトップにショートカットがない場合は、手動で作成してください:
1. デスクトップを右クリック →「新規作成」→「ショートカット」
2. 場所に `C:\mail-reader\start.bat` と入力
3. 名前を「メール読み上げ」に設定

起動したら自動的に未読メールを読み始めます。動くことが確認出来たらデスクトップ上の`install.bat` と `.env` は削除してかまいません。

## トラブルシューティング

### 「python が見つかりません」と表示される

→ install.bat を管理者として再実行してください。

### 「未読メールはありません」と表示されるが実際には未読がある

→ `C:\mail-reader\.env` のメールアドレス・パスワードを確認してください。
→ 直近30日間の未読メールのみが対象です。

### 音声が出ない

→ PCの音量を確認してください。
→ Edge-TTS はインターネット接続が必要です。

### 返信が生成されない（「返信を考えています」のまま止まる）

→ 開発元にお問い合わせください。

## アップデート

自動です。デスクトップのショートカットから起動するたびに、最新版を自動取得します。

## お問い合わせ

開発責任者：谷川郁男
Mail：tanikawa@tranzact.co.jp
Mobile：090-4194-4069
