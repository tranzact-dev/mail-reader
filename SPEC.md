# メール読み上げアプリ 仕様書

## 概要

高齢の利用者（80歳、指がほとんど動かない）向けに、BIGLOBEメールの未読メールを音声で読み上げ、ボタン操作だけで返信まで完結できるデスクトップアプリケーション。

## 対象環境

| 項目 | 値 |
|------|------|
| OS | Windows 10 / 11 |
| Python | 3.13+ |
| メールサーバー | BIGLOBE (mail.biglobe.ne.jp) |
| AI | Claude API (claude-sonnet-4-6) |
| 音声合成 | Edge-TTS (ja-JP-NanamiNeural) |

## 機能一覧

### 1. メール受信・表示

- IMAP4 SSL接続でBIGLOBEメールサーバーに接続
- 直近30日間の未読メールを取得
- 同一件名のメールをスレッドとしてグループ化（Re:/Fwd:/転送: を正規化）
- メールヘッダー（差出人・日時・件名）と本文をダークテーマで表示
- 添付ファイルの有無を表示
- HTML メールはタグ・スタイル・スクリプトを除去してプレーンテキスト化
- 引用部分（`>` 行、転送ヘッダー等）を自動除去
- 本文上限: 10,000文字

### 2. 音声読み上げ

- Edge-TTS による日本語音声合成（Nanami Neural）
- 文単位のハイライト同期（読み上げ中の文を黄色背景で強調）
- 読み上げ内容: スレッド番号 → 差出人 → 件名 → 添付有無 → 本文
- スレッド内の複数メールは連続再生
- 全メール再生完了時に「全てのメールを読み終わりました」と通知

### 3. AI返信生成

- Claude API (claude-sonnet-4-6) による返信文自動生成
- 返信の方向性を4種から選択:
  - **承諾**: 相手の提案・依頼を了承
  - **辞退**: やんわり見送り
  - **保留**: 確認して後日連絡
  - **おまかせ**: AIが文脈判断
- 同一差出人の過去メール（最大5件）を文脈として参照
- トーン自動切替:
  - 丁寧語（デフォルト）: 「〜です」「〜します」
  - くだけた口調: `CASUAL_CONTACTS` に登録された差出人向け
- 生成上限: 300文字以内
- 下書きは画面上で直接編集可能（黄色カーソル表示）
- 「やり直す」で再生成可能

### 4. メール送信

- SMTP SSL接続 (mail.biglobe.ne.jp:465) で返信送信
- `Re:` 件名、`In-Reply-To` / `References` ヘッダー自動設定
- 送信後、IMAPの送信済みフォルダにコピーを保存

### 5. 添付ファイル

- 添付ファイルをテンポラリフォルダにダウンロードし、OS既定アプリで開く
- 添付ありメール表示時のみ「添付を開く」ボタンを表示
- 除外対象: `winmail.dat`, `smime.p7s`（技術的メタデータ）

### 6. その他

- 文字サイズ変更（A+ / A-ボタン、14pt〜72pt、デフォルト46pt）
- メール一覧の更新（再接続＋再取得）
- アプリ終了時にIMAP接続をクローズ

## UI構成

### 画面レイアウト

```
+---------------------------------------------+
| [状態表示]                        [A+] [A-] |  <- ステータスバー
+---------------------------------------------+
| 差出人: XXXX                                |
| 日時: 2025年01月01日 12:00                   |
| 件名: XXXXについて                           |
| 添付ファイルあり                              |
|                                             |
| 本文エリア（スクロール可能）                   |
|                                             |
+---------------------------------------------+
| [再生] [停止] [前へ] [聞き直す] [次へ]       |  <- 操作ボタン
| [添付を開く] [返信] [更新] [終了]            |
+---------------------------------------------+
```

### 画面遷移

```
メール画面 --[返信]--> 方向性選択画面 --[承諾/辞退/保留/おまかせ]--> 返信画面
    ^                     |                                           |
    +------[戻る]---------+                                           |
    ^                                                                 |
    +------[戻る]---------------------------------------------------------+
    ^                                                                 |
    +------[送信完了]-----------------------------------------------------+
```

### ボタン仕様

- フォント: Meiryo UI 20pt
- サイズ: width=12, height=2（大型、押しやすい）
- ダークテーマ（背景 #1a1a2e）
- キーボードのEnter/Spaceでも操作可能

## 設定ファイル (.env)

```
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx    # Claude APIキー
BIGLOBE_EMAIL=xxxxx@kyf.biglobe.ne.jp   # BIGLOBEメールアドレス
BIGLOBE_PASSWORD=xxxxx                   # BIGLOBEパスワード
CASUAL_CONTACTS=a@example.com,b@example.com  # くだけた口調にする差出人メールアドレス（カンマ区切り）
```

## 技術アーキテクチャ

### ファイル構成

```
mail-reader/
├── mail_reader_gui.py   # メインアプリケーション（単一ファイル）
├── start.bat            # 起動ランチャー（git pull + アプリ起動）
├── install.bat          # 自動インストーラー
├── requirements.txt     # Python依存パッケージ
├── .env                 # 環境設定（git管理外）
├── SPEC.md              # 本仕様書
├── INSTALL.md           # インストール手順
└── docs/                # ブラウザ版（GitHub Pages、参考用）
```

### 依存パッケージ

| パッケージ | 用途 |
|-----------|------|
| anthropic | Claude API クライアント |
| edge-tts | Microsoft Edge 音声合成 |
| pygame | 音声ファイル再生 |
| python-dotenv | .env ファイル読み込み |
| tkinter | GUI（Python標準ライブラリ） |

### 処理フロー

1. 起動 → `.env` 読み込み → IMAP接続 → 未読メール取得
2. スレッドグループ化 → 最初のメール表示 → 自動読み上げ開始
3. ユーザー操作（ボタン）に応じて次へ/前へ/停止/返信等
4. 返信時: 方向性選択 → Claude API呼び出し → 下書き表示・読み上げ → 編集 → SMTP送信
5. 終了時: IMAP切断 → ウィンドウ破棄

### DRY_RUN モード

`mail_reader_gui.py` 内の `DRY_RUN` フラグ（現在は `False` = 本番モード）:
- `True` の場合: メール送信をスキップ、既読フラグを設定しない（開発・テスト用）
- `False` の場合: 通常動作（送信・既読マーク有効）

## 今後の拡張予定

- **論点分析＋選択式返信**: AIがメール内の論点を抽出し、選択肢をボタンで提示。ステップ形式で回答を選ぶだけで返信が組み立てられる機能。
- **exe化**: PyInstallerによる実行ファイル生成（Python環境不要で起動可能に）
